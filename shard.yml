name: appkit
version: 0.1.3

authors:
  - Philippe <papilip@github>

description: |
  Crystal bindings for macOS AppKit.
  Native menus, dialogs, notifications, status bar, and more.

crystal: ">= 1.15.0"

license: MIT

scripts:
  postinstall: cd ext && make

development_dependencies:
  ameba:
    github: crystal-ameba/ameba
    version: "~> 1.6"
